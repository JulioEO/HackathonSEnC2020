<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistemassalasinteligentes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ssilogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SistemasSalasInteligentes">
        <activity android:name=".DateTimePicker" />
        <activity android:name=".RoomList" />
        <activity android:name=".BlocosActivities" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReservarSalaActivity" />
    </application>

</manifest>